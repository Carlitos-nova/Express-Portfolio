<!--Main Navigation-->
<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container-fluid">
      <a class="navbar-brand" href="/">Carlitos de Castro <img src="Assets/images/Carl-Logo.png" height="100" alt="Logo"></a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarSupportedContent">
        <ul class="navbar-nav ml-auto mb-2 mb-lg-0">
          <% if (title=="Home") {%>
            <li class="nav-item active"></li>
            <% } else { %>
          <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/"><i class="fas fa-lg fa-house-user"></i> Home</a>
          </li>

          <% if (title=="About") {%>
            <li class="nav-item active"></li>
            <% } else { %>
          <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/about"><i class="fas fa-info"></i> About</a>
          </li>

          <% if (title=="Services") {%>
            <li class="nav-item active"></li>
            <% } else { %>
          <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/services"><i class="fas fa-concierge-bell"></i> Services</a>
          </li>

          <% if (title=="Contact") {%>
            <li class="nav-item active"></li>
            <% } else { %>
          <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/contact"><i class="fab fa-twitter"></i> Contact</a>
          </li>

          <% if (title=="Products") {%>
            <li class="nav-item active"></li>
            <% } else { %>
          <li class="nav-item">
            <% } %>
            <a class="nav-link " aria-current="page" href="/products"><i class="fab fa-playstation"></i> Products</a>
          </li>
    </div>
  </nav>